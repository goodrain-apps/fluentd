<source>
@type tail
format none
path ${mysql_log}/error.log
pos_file ${mysql_log}/error_containers.pos
  <parse>
    @type none
  </parse>
tag mysql_error_log
</source>

<source>
@type tail
format none
path ${mysql_log}/slow.log
pos_file ${mysql_log}/slow_containers.pos
  <parse>
    @type none
  </parse>
tag mysql_slow_log
</source>

