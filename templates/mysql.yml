<source>
@type tail
format none
path ${MYSQL_ERROR_PATH}
pos_file /var/log/error_containers.pos
  <parse>
    @type none
  </parse>
tag mysql_error_log
</source>

<source>
@type tail
format none
path ${MYSQL_SLOW_PATH}
pos_file /var/log/slow_containers.pos
  <parse>
    @type none
  </parse>
tag mysql_slow_log
</source>

